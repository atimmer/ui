{
  "name": "ui",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp run -r build && vp check && vp run -r test",
    "dev": "vp run @24letters/ui-showcase#dev",
    "build": "vp run -r build",
    "test": "vp run -r test"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.15.1",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
